/* eslint-disable prettier/prettier */
export class CreateUserProfileDto {
  firstName: string;
  lastName: string;
  age: number;
  userId: number; // Assuming userId is passed when creating a profile
}
